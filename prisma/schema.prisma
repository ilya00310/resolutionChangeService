
generator client {
  provider = "prisma-client-js"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model VideoProcess {
  id  Int   @id   @default(autoincrement())
  video_id String
  processing_stage String

  @@unique([video_id])
}